import React from "react";
import { withTranslation } from "react-i18next";

const LoginOrSignUpContainer = ({ t, ...rest }) => {
  return <></>;
};

export default withTranslation("pdp")(LoginOrSignUpContainer);
