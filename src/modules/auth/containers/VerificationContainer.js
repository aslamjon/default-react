import React, { memo } from "react";
import { withTranslation } from "react-i18next";

const VerificationContainer = ({ t, request, ...rest }) => {
  return <></>;
};

export default withTranslation("pdp")(memo(VerificationContainer));
