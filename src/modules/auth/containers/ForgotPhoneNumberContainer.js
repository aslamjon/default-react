import React, { memo } from "react";
import { withTranslation } from "react-i18next";

const ForgotPhoneNumberContainer = ({ request, t, history, match, ...props }) => {
  return <></>;
};

export default withTranslation("pdp")(memo(ForgotPhoneNumberContainer));
